@model IEnumerable<MBAModulo1.Core.Models.Categoria>

@{
    ViewData["Title"] = "Lista de Categorias";
    Layout = "_Layout";
}

<h2>Categorias</h2>

<a asp-action="Create" class="btn btn-success mb-3">Nova Categoria</a>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var categoria in Model)
        {
            <tr>
                <td>@categoria.Nome</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@categoria.Id" class="btn btn-sm btn-primary">Alterar</a>
                    <a asp-action="Delete" asp-route-id="@categoria.Id" class="btn btn-sm btn-danger" onclick="return confirm('Tem certeza que deseja excluir esta Categoria?');">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
